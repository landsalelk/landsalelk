# Palette's Journal

## 2024-05-22 - Accessibility in Filters
**Learning:** Filter components often rely on implicit labels (group labels) which fail WCAG for individual inputs like min/max price.
**Action:** Always add explicit `aria-label` to range inputs and ensure toggle buttons have `aria-expanded` states.
